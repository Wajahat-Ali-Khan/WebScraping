
def retrieve(self, request, *args, **kwargs):
    instance = self.get_object()
    serializer = self.get_serializer(instance)
    return Response(serializer.data) Brand:
    def __init__(self, name , model):
        self.name = name
        self.model = model
        def details(self):
            print(self.name, + 'is' + self.model)

def retrieve(self, request, *args, **kwargs):
    instance = self.get_object()
    serializer = self.get_serializer(instance)
        
    def app():
        return Response(serializer.data)


if 2>1:
    print("adas")
    if 2<1:
        print("sadsad")
else:
    print("ajdnbasd")


